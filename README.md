Simple use of mediapipe and cv2
The mouse moves according to your index finger and when you touch your little finger and thumb, left click is triggered

This works only for mac systems as it uses quartz module of macos for mouse actions
Ensure that `pip install pyobjc-framework-Quartz` is done
